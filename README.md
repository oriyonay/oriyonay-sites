# Ori Yonay — Web Design

Static site. Open `index.html` in a browser, or from this folder:

```bash
python3 -m http.server 8000
```

Then go to [http://127.0.0.1:8000](http://127.0.0.1:8000).
# oriyonay-sites
